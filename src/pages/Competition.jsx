import { useParams } from "react-router-dom";

const Competition = () => {
  const params = useParams();
  const { id } = params;

  return <div>Competition Azertyyyy</div>;
};

export default Competition;
